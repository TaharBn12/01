interface:
  display_name: "Flutter Billing App"
  short_description: "Repo-specific patterns and workflows for flutter_billing_app"
  default_prompt: "Use the flutter_billing_app repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true